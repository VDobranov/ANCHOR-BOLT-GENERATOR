# ANCHOR-BOLT-GENERATOR

Генератор анкерных болтов (фундаментных болтов) по ГОСТ 24379.1-2012 с экспортом в IFC и 3D визуализацией в браузере.

## Быстрый старт

### Запуск

```bash
python3 -m http.server 8000
```

Откройте в браузере: **http://localhost:8000**

### Использование

1. Выберите тип болта (1.1, 1.2, 2.1 или 5)
2. Выберите диаметр (М12–М48)
3. Выберите длину (из доступных по ГОСТ)
4. Выберите материал (09Г2С, ВСт3пс2 или 10Г2)
5. Настройте параметры экспорта IFC (опционально)
6. Вращайте 3D-вид мышью (ЛКМ — вращение, колесо — зум)
7. Нажмите «Скачать IFC» для экспорта

## Настройки экспорта IFC

### Класс сборки

Определяет тип IFC-сущности для болта:

- **IfcMechanicalFastener** (по умолчанию) — стандартный класс для крепежа
- **IfcElementAssembly** — класс для сборок элементов

### Режим формирования

Определяет способ представления геометрии:

- **Вроссыпь** (по умолчанию) — каждый компонент (шпилька, гайки, шайба, плита) создаётся как отдельный объект IFC
- **Всё одним телом** — все компоненты объединяются в один объект с помощью булевых операций

### Тип геометрии

Определяет способ представления 3D-геометрии:

- **Твердотельная** (по умолчанию) — параметрическая геометрия через `IfcExtrudedAreaSolid`, `IfcSweptDiskSolid`, `IfcCSGSolid`
- **Поверхностная** — полигональная геометрия через `IfcFacetedBrep`

## Параметры болтов

### Типы и размеры

| Тип и исполнение | Диаметры | Длины | Состав сборки |
|-----------------|----------|-------|---------------|
| 1.1 | М12–М48 | 300–2800 | Шпилька + шайба + 2 гайки |
| 1.2 | М12–М48 | 300–2800 | Шпилька + шайба + 2 гайки |
| 2.1 | М16–М48 | 200–2800 | Шпилька + анкерная плита + шайба + 4 гайки |
| 5 | М12–М48 | 150–1400 | Шпилька + шайба + 2 гайки |

**Примечание:** Доступные длины зависят от диаметра и типа болта. Полный список генерируется автоматически из данных по ГОСТ.

### Материалы

| Материал | ГОСТ | Описание |
|----------|------|----------|
| 09Г2С | 19281-2014 | Низколегированная сталь (490 МПа) |
| ВСт3пс2 | 535-88 | Углеродистая сталь (345 МПа) |
| 10Г2 | 19281-2014 | Низколегированная сталь (490 МПа) |

### Система координат

Все болты расположены таким образом, чтобы отметка низа резьбы находилась в начале координат (0, 0, 0), а ось болта была ориентирована вдоль оси Z.

## Технологии

- **Three.js r128** — 3D визуализация в браузере
- **Pyodide 0.26.0** — Python в браузере (WebAssembly)
- **IfcOpenShell 0.8.4** — создание IFC файлов
- **IFC4 ADD2 TC1** — стандарт BIM-моделей

## Валидация buildingSMART

Проект проходит валидацию правилами [buildingSMART ifc-gherkin-rules](https://github.com/buildingSMART/ifc-gherkin-rules).

### Результаты валидации

```
✅ 86 features passed
❌ 6 features failed (требуют анализа)
⏭️  8 features skipped

✅ 355 scenarios passed
❌ 6 scenarios failed

✅ 1481 steps passed
❌ 6 steps failed
```

### Запуск локальной валидации

```bash
# Установка зависимостей
pip install ifcopenshell behave
pip install -r external/ifc-gherkin-rules/requirements.txt

# Генерация тестового файла
PYTHONPATH=python:$PYTHONPATH python -c "
from main import initialize_base_document, reset_doc_manager
from instance_factory import InstanceFactory
reset_doc_manager()
ifc_doc = initialize_base_document('test')
builder = InstanceFactory(ifc_doc, geometry_type='solid')
builder.create_bolt_assembly('1.1', 20, 800, '09Г2С', 'IfcMechanicalFastener', 'separate', 'solid')
ifc_doc.write('test.ifc')
"

# Запуск валидации
cd external/ifc-gherkin-rules
python -m behave --define input=../../test.ifc --tags="-@disabled" features/rules/
```

### CI/CD

Валидация автоматически запускается при каждом push и pull request через GitHub Actions.
См. [.github/workflows/ifc-validation.yml](.github/workflows/ifc-validation.yml).

## Лицензия

MIT

Разработано с помощью AI-ассистентов (Claude, Qwen)
