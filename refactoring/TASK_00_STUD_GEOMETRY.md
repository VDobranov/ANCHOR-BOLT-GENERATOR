# 📋 Task 0: Исправление геометрии шпильки типа 2.1

**Файл:** `python/geometry_builder.py`  
**Длительность:** 30 минут  
**Приоритет:** 🔴 Критичный  
**Статус:** ⏳ Ожидает

---

## 🎯 Цель

Исправить геометрию шпильки типа 2.1 так, чтобы Z=0 было на отметке **начала резьбы** (аналогично типам 1.1, 1.2, 5).

---

## ✅ Чек-лист

- [ ] 0.1. Прочитать текущую реализацию `create_composite_curve_stud` для типа 2.1
- [ ] 0.2. Найти функцию `get_thread_length` для получения длины резьбы l0
- [ ] 0.3. Исправить геометрию типа 2.1: от Z=+l0 до Z=-(L-l0)
- [ ] 0.4. Запустить тесты геометрии (`tests/test_geometry_builder.py`)
- [ ] 0.5. Запустить все тесты проекта
- [ ] 0.6. Закоммитить изменения

---

## 📝 Детали реализации

### Текущая проблема

```python
# Тип 2.1: прямая шпилька от (0, 0, length) до (0, 0, 0)
points = [V(0.0, 0.0, float(length)), V(0.0, 0.0, 0.0)]
return self.builder.polyline(points)
```

**Недостаток:** Низ шпильки в Z=0, а не начало резьбы.

### Требуемое решение

```python
# Тип 2.1: прямая шпилька с резьбой по всей длине
# Низ резьбы в Z=0 (аналогично типам 1.1, 1.2 и 5)
if bolt_type == "2.1":
    # Получаем длину резьбы
    l0 = get_thread_length(diameter, length) or length
    
    # Тип 2.1: прямая шпилька длиной L с резьбой l0
    # Низ резьбы в Z=0, значит:
    # - Верх шпильки (конец резьбы): Z = +l0
    # - Низ шпильки: Z = -(L - l0)
    # Общая длина: l0 + (L - l0) = L
    p1 = V(0.0, 0.0, float(l0))
    p2 = V(0.0, 0.0, float(-(length - l0)))
    
    return self.builder.polyline([p1, p2])
```

---

## 🧪 Тесты для проверки

После изменения проверить:
- Тип 2.1: шпилька начинается на Z=0 (начало резьбы)
- Тип 2.1: верх шпильки на Z=+l0
- Тип 2.1: низ шпильки на Z=-(L-l0)
- Остальные типы (1.1, 1.2, 5) работают без изменений

---

## 🔗 Ссылки

- [PHASE_PLATE_TODO.md](./PHASE_PLATE_TODO.md#0-исправлен
